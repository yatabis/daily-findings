# AGENTS.md

このリポジトリは、ChatGPTで動かしている各種ウォッチ系スケジュールタスクのうち、保存・公開する価値がある出力を蓄積し、Webサイトとして公開するためのものです。

## 基本ルール

- リポジトリ内に残す自然言語の文章は日本語で書く。
- 公開データの基本単位は `Finding` とする。
- 各ウォッチの全実行結果を保存せず、何かしら外に残す価値がある更新・変化があった場合だけFindingを追加する。
- `status` はFindingに対する任意の判断として `TRY` / `WATCH` / `HOLD` を使う。
- `EXCLUDE` は原則として公開せず、Finding自体を作らない。
- ユーザーがFindingごとに追加の評価や記録作業を行うことは前提にしない。
- `Topic` や `Assessment` などの新しい恒久データモデルは、実際のFinding運用から必要性が確認できるまでは導入しない。
- 不要な抽象化や将来設計の先取りを避け、現在の運用に必要な最小構成を優先する。
- READMEはリポジトリの入口として簡潔に保つ。サイト利用者向けの説明や長い設計思想はREADMEへ置かない。
- Cloudflare、Web UI、API、WebMCP、AEO計測などの実装は、このデータモデルを利用する側として扱う。

## 現在の構成

```text
content/
  findings/
schema/
  finding.schema.json
```
