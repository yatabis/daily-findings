{
  "name": "daily-findings",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "test": "node --test tests/publication.test.mjs",
    "build": "npm test && astro build && node scripts/verify_web_build.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "7.3.1"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
